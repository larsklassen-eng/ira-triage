[project]
name = "custodian-challenge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Lars Klassen", email = "lars.klassen@factored.ai" }
]
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.120.2",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.2",
]

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "ira_triage"
